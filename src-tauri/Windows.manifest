<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
  Windows app manifest. Phase 2 stub.

  - requestedExecutionLevel="asInvoker": no UAC prompt at app launch. Phase 6
    (TUN mode) will trigger UAC on demand via a separate elevated helper, NOT
    by elevating the whole app.
  - DPI awareness: per-monitor v2 so the React UI scales correctly on
    multi-monitor setups.
-->
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v3">
    <security>
      <requestedPrivileges>
        <requestedExecutionLevel level="asInvoker" uiAccess="false"/>
      </requestedPrivileges>
    </security>
  </trustInfo>
  <application xmlns="urn:schemas-microsoft-com:asm.v3">
    <windowsSettings>
      <dpiAwareness xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">PerMonitorV2</dpiAwareness>
      <longPathAware xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">true</longPathAware>
    </windowsSettings>
  </application>
</assembly>
